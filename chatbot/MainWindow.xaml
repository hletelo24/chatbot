<Window x:Class="chatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:chatbot"
        mc:Ignorable="d"
        Title="CyberGuard Bot - Cybersecurity Awareness" 
        Height="700" Width="1000"
        Background="#1E1E2F" Foreground="#E0E0E0"
        Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="650"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Chat Area -->
        <GroupBox Header="Chat with CyberGuard" FontWeight="Bold" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ASCII Art -->
                <TextBlock x:Name="txtAscii" Grid.Row="0" FontFamily="Consolas" 
                           FontSize="12" TextWrapping="Wrap" Margin="5"
                           Foreground="#00FFAA" HorizontalAlignment="Center"/>

                <!-- Chat History -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="chatHistory" TextWrapping="Wrap" Margin="5"
                               FontSize="14" LineHeight="22"/>
                </ScrollViewer>

                <!-- Input -->
                <Grid Grid.Row="2" Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtInput" KeyDown="txtInput_KeyDown"
                             FontSize="14" Padding="8" Background="#2D2D44" Foreground="#E0E0E0"/>
                    <Button Grid.Column="1" Content="Send" Width="80" Margin="5,0,0,0"
                            Click="btnSend_Click" Background="#00AAFF" Foreground="White"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Sidebar -->
        <StackPanel Grid.Column="1" Margin="10">
            
            <Button Content="Show Activity Log" Height="50" Margin="5" Click="btnLog_Click" Background="#AA66FF"/>
            <Button Content="Clear Chat" Height="40" Margin="5" Click="btnClear_Click"/>
        </StackPanel>
    </Grid>
</Window>
